{
  "name": "timeout-quirurgico-api",
  "version": "1.0.0",
  "private": true,
  "description": "API compartida del Time Out Quirúrgico (CIMA): permite que distintas personas (preanestesia, quirófano) continúen el mismo registro.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "pg": "^8.11.5"
  }
}
